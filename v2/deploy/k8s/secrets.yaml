# Apply this MANUALLY after filling in values.
# Never commit real secrets.  Use kubectl create secret or a secrets manager.
#
#   kubectl create secret generic jarvis-secrets \
#     --from-literal=database_url="postgresql+asyncpg://..." \
#     --from-literal=redis_url="redis://..." \
#     --from-literal=secret_key="$(openssl rand -hex 32)" \
#     --from-literal=openrouter_api_key="..." \
#     -n jarvis-v2
#
# Shown here as a template only — values are base64("placeholder")
apiVersion: v1
kind: Secret
metadata:
  name: jarvis-secrets
  namespace: jarvis-v2
type: Opaque
data:
  database_url: cGxhY2Vob2xkZXI=
  redis_url: cGxhY2Vob2xkZXI=
  secret_key: cGxhY2Vob2xkZXI=
  openrouter_api_key: cGxhY2Vob2xkZXI=
